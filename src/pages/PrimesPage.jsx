import { PrimesTemplate } from "../components/templates/index.js";

/**
 * Page: PrimesPage
 * Page de vérification des nombres premiers
 * Utilise le PrimesTemplate
 */
export function PrimesPage() {
  return <PrimesTemplate />;
}
